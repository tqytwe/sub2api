---
description: Sub2API 禁止本地起服务，必须通过推送后在 Zeabur 线上验收
alwaysApply: true
---

# Sub2API — 线上验收（本项目）

**禁止**为验收启动本地 `go run serve`、`pnpm dev` 或访问 `localhost`。

部署流程（推送到 GitHub，由 Zeabur 自动构建）：

```bash
./scripts/push-github-and-deploy.sh
```

**验收地址**：**https://www.jisudeng.com/**（前端 + API / Admin / `/api/v1/...`）。

- 用户侧 UI（Play / 增长世界 / 仪表盘 / 图像工作室）：浏览器走查浅色 + 深色模式
- Admin 密码以 Zeabur / 线上环境变量为准
- **不再使用** Dell / `192.168.100.10` 做部署或验收

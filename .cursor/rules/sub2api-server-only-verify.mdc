---
description: Sub2API 服务器开发验证、Zeabur 部署与用户本地电脑生产验收
alwaysApply: true
---

# Sub2API 交付与验收

完整规则以 `docs/DELIVERY_WORKFLOW.md` 为准。

## 服务器阶段

- 使用隔离 Git worktree 开发。
- 业务行为严格 TDD，每个任务依次完成规格审查和代码质量审查。
- 定向测试、完整测试、完整构建和 `./scripts/check-fork-integrity.sh` 全通过后才能提交和推送。
- 先推送审查分支并创建目标为 `play/main` 的 PR；完整 GitHub CI 只在 PR 执行一次。
- `origin/play/main` 触发 Zeabur，不重复等待完整测试；确认实际部署 commit、健康状态和关键 API 后才能验收。

## 本地电脑验收

- 最终产品验收由用户在本地电脑浏览器访问 `https://www.jisudeng.com/`。
- 至少覆盖游客、普通用户、管理员三种身份；相关功能同时检查用户和管理员页面。
- 视觉相关修改检查浅色和深色，数据相关修改按需进行 API/数据库对账。
- 服务器浏览器、curl 和健康检查只是技术证据，不能替代用户本地电脑验收。

**禁止**为最终验收启动 `go run serve`、`pnpm dev` 或访问 localhost。服务器仍允许且必须运行测试与 production build。

登录密码、API key、Cookie 和生产凭据只通过环境变量或人工输入提供，不写入 Git、代码、文档、命令输出或日志。每次交付记录 commit、测试、构建、部署和浏览器验收证据；失败或缺证据时不得宣称完成。
